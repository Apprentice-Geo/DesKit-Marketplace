{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/WiIIiamWei/DesKit-Marketplace/schema/registry.schema.json",
  "title": "DesKit Marketplace registry index",
  "description": "Generated index the DesKit client fetches over HTTPS. Built from plugins/*.json — do not hand-edit when a build step is in place.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "plugins"],
  "properties": {
    "version": {
      "type": "integer",
      "const": 1,
      "description": "Registry format version. Bump when the entry shape changes incompatibly."
    },
    "plugins": {
      "type": "array",
      "items": {
        "$ref": "registry-entry.schema.json"
      }
    }
  }
}
